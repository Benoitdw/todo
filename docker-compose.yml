services:
  todo:
    image: ghcr.io/benoitdw/todo-server:latest
    ports:
      - "8080:8080"
    environment:
      - BEARER_TOKEN=change-me
    volumes:
      - ./data:/data
    restart: unless-stopped
