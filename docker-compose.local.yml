services:
  todo:
    build:
      context: .
      dockerfile: todo-server/Dockerfile
    ports:
      - "8080:8080"
    environment:
      - BEARER_TOKEN=change-me
    volumes:
      - ./data:/data
    restart: unless-stopped
